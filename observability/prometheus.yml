global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: wagering-api
    static_configs:
      - targets:
          - host.docker.internal:3000
        labels:
          service: wagering-api
    metrics_path: /metrics
